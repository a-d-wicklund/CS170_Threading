#include <stdio.h>
#include <stdlib.h>
#include <pthread.h>


int main(){
    //Create a new thread to run a function that takes a while

    pthread_t tid;
    pthread_attr attr;
    prhtread_attr_init(&attr);

    pthread_create(&tid, )


}
